{% extends 'base.back.html.twig' %}

{% block title %}Modifier Film
{% endblock %}

{% block body %}
	<a href="{{ path('app_admin_film_index') }}" class="btn btn-inverse-secondary mb-3">Voir la liste</a>
	<div class="d-flex justify-content-between">
	    <h1>Modifier le film</h1>
		{{ include('admin_film/_delete_form.html.twig') }}
	</div>

	{{ include('admin_film/_form.html.twig', {'button_label': 'Enregistrer'}) }}


{% endblock %}
