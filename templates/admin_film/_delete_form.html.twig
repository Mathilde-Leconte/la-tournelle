<form method="post" action="{{ path('app_admin_film_delete', {'id': film.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce film?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ film.id) }}">
    <button class="btn btn-inverse-danger me-2">Supprimer</button>
</form>
